cwd: /workspaces/cinema-firmware-tracker

======================================================================
canon_cameras.json
======================================================================
rows: 22

union of keys across all 22 rows:
   make                     non-empty in 22/22 rows
   model                    non-empty in 22/22 rows
   version                  non-empty in 22/22 rows
   date                     non-empty in 22/22 rows
   download_url             non-empty in 22/22 rows
   download_opens_page      non-empty in 22/22 rows
   source_url               non-empty in 22/22 rows
   release_notes_url        non-empty in 21/22 rows
   changelog                non-empty in 21/22 rows
   install_steps            non-empty in 9/22 rows
   previous_versions        non-empty in 19/22 rows

----------------------------------------------------------------------
RICHEST ROW, full shape: ?
----------------------------------------------------------------------
  .make = str(5) Canon
  .model = str(16) EOS C500 Mark II
  .version = str(7) 1.1.5.1
  .date = str(10) 2025-05-21
  .download_url = str(78) https://www.usa.canon.com/support/p/eos-c500-mark-ii?subtab=downloads-firmware
  .download_opens_page = bool True
  .source_url = str(52) https://www.usa.canon.com/support/p/eos-c500-mark-ii
  .release_notes_url = str(116) https://www.usa.canon.com/support/canon-product-advisories/Firmware-Notice-EOS-C500-Mark-I...
  .changelog = str(1081) Enables the display of the digital tele-converter icon on [Main Recording Displays] of [DI...
  .install_steps = str(2841) Caution: - Please use an SD/SDHC/SDXC memory card that is 512MB or greater to perform the ...
  .previous_versions = list(6), first item:
      dict(6 keys)
          .version = str(7) 1.1.2.1
          .date = str(10) 2024-07-03
          .download_url = str(78) https://www.usa.canon.com/support/p/eos-c500-mark-ii?subtab=downloads-firmware
          .download_opens_page = bool True
          .release_notes_url = str(116) https://www.usa.canon.com/support/canon-product-advisories/Firmware-Notice-EOS-C500-Mark-I...
          .changelog = str(208) 1. Adds [Sensor mode] as an option to assignable buttons.\n2. Adds the following size opti...

----------------------------------------------------------------------
SPARSEST ROW, full shape: ?
----------------------------------------------------------------------
  .make = str(5) Canon
  .model = str(16) EOS C100 Mark II
  .version = str(7) 1.0.8.1
  .date = str(10) 2017-03-29
  .download_url = str(78) https://www.usa.canon.com/support/p/eos-c100-mark-ii?subtab=downloads-firmware
  .download_opens_page = bool True
  .source_url = str(52) https://www.usa.canon.com/support/p/eos-c100-mark-ii
  .release_notes_url = str(0) 
  .changelog = str(0) 
  .install_steps = str(0) 
  .previous_versions = list(3), first item:
      dict(6 keys)
          .version = str(7) 1.0.5.1
          .date = str(10) 2017-08-28
          .download_url = str(78) https://www.usa.canon.com/support/p/eos-c100-mark-ii?subtab=downloads-firmware
          .download_opens_page = bool True
          .release_notes_url = str(119) https://www.usa.canon.com/support/canon-product-advisories/firmware-notice-eos-c100-mark-i...
          .changelog = str(1041) EOS C100 Mark II: Firmware Version 1.0.5.1.00 Firmware Version\n1. 0 .5 . 1.00 incorporate...

======================================================================
build_feed.py
======================================================================
--- lines 174-202 ---
     174| # ---- RED ----
     175| # red_cameras.py already writes feed-shaped rows. RED gates the file behind
     176| # a login, so firmware_url points at RED's own page, not a direct download.
     177| try:
     178|     with open("red_cameras.json") as f:
     179|         for row in json.load(f):
     180|             feed.append({
     181|                 "manufacturer": "RED",
     182|                 "product": row["product"],
     183|                 "version": row["version"],
     184|                 "release_date": row["release_date"],
     185|                 "release_precision": row.get("release_precision") or "day",
     186|                 "status": row["status"],
     187|                 "source_url": row["source_url"],
     188|                 "firmware_url": row.get("firmware_url"),
     189|                 "firmware_kind": row.get("firmware_kind") or "page",
     190|                 "notes_url": row.get("notes_url"),
     191|                 "archive_url": None,
     192|                 "summary": row.get("summary"),
     193|                 "features": row.get("features") or [],
     194|                 "install": row.get("install") or [],
     195|                 "install_version": row.get("install_version"),
     196|                 "install_source": row.get("install_source"),
     197|                 "file_name": row.get("file_name"),
     198|                 "file_size": row.get("file_size"),
     199|             })
     200| except FileNotFoundError:
     201|     print("red_cameras.json not found, skipping RED")
     202| 

======================================================================
build_site.py
======================================================================
--- lines 560-600 ---
     560|     else:
     561|         where = "card"
     562| 
     563|     strip = {"file": file_name, "where": where, "menu": menu}
     564|     if slot:
     565|         strip["slot"] = "slot " + slot
     566|     return strip
     567| 
     568| def state_of(cam):
     569|     """What a tech actually gets if they click. Honest outcomes only.
     570| 
     571|     A "page" link is not a download: Sony's FX bodies want a file copied to
     572|     a card, and RED puts the firmware behind an account login. Say so
     573|     rather than promising a file.
     574|     """
     575|     kind = cam.get("firmware_kind")
     576|     make = cam.get("manufacturer")
     577|     has_file = bool(cam.get("firmware_url"))
     578|     has_notes = bool(cam.get("notes_url"))
     579| 
     580|     if has_file and kind == "page":
     581|         if make == "RED":
     582|             return "Login required for download", OLIVE
     583|         if make == "Sony":
     584|             return "Card update via Sony", OLIVE
     585|         if has_notes:
     586|             return "Firmware + notes", ORANGE
     587|         return "Firmware", ORANGE
     588|     if has_file and has_notes:
     589|         return "Firmware + notes", ORANGE
     590|     if has_file:
     591|         # Sony ships the file without a scrapeable changelog.
     592|         return "Firmware", ORANGE
     593|     if has_notes:
     594|         return "Documentation only", OLIVE
     595|     return "Version only", GREY
     596| 
     597| 
     598| def sort_key(cam):
     599|     return cam.get("release_date") or ""
     600| 

--- lines 600-664 ---
     600| 
     601| 
     602| # ARRI's page headlines are the source of the version, but they are not always
     603| # the name a tech uses for the body. ARRI's own release notes and user manual
     604| # for SUP 11.1.1 call it "ALEXA Classic" (the h1 just says "ALEXA"), and the
     605| # camera index writes the Mini bodies in mixed case. Nothing here invents a
     606| # product: each value is the manufacturer's own wording for the same camera.
     607| DISPLAY_NAMES = {
     608|     "ALEXA": "ALEXA Classic",
     609|     "ALEXA MINI": "ALEXA Mini",
     610|     "ALEXA MINI LF": "ALEXA Mini LF",
     611|     "AMIRA LIVE / AMIRA": "AMIRA",
     612| }
     613| 
     614| 
     615| def display_name(product):
     616|     """Manufacturer's own name for the body, tidied for reading."""
     617|     if not product:
     618|         return ""
     619|     name = " ".join(product.split())
     620|     fixed = DISPLAY_NAMES.get(name.upper())
     621|     if fixed:
     622|         return fixed
     623|     # Sony ships model codes bolted onto the name, e.g. FX3(ILME-FX3A).
     624|     name = re.sub(r"(?<=[^ (])\(", " (", name)
     625|     return name
     626| 
     627| 
     628| def panel_payload(cam, name, label):
     629|     """Everything the panel shows for one camera, as plain data."""
     630|     bench = bench_line(cam)
     631|     steps = []
     632|     for block in cam.get("install") or []:
     633|         items = [item for item in (block.get("items") or []) if item]
     634|         if items:
     635|             steps.append({"h": block.get("heading") or "", "i": items})
     636| 
     637|     links = []
     638|     for text, href in panel_links(cam):
     639|         if href:
     640|             links.append({"t": text, "u": href})
     641| 
     642|     return {
     643|         "make": cam.get("manufacturer") or "",
     644|         "name": name,
     645|         "version": cam.get("version") or "",
     646|         "date": date_label(cam.get("release_date"), cam.get("release_precision")),
     647|         "size": cam.get("file_size") or "",
     648|         "state": label,
     649|         "bench": bench,
     650|         "news": cam.get("changelog") or cam.get("features") or [],
     651|         "steps": steps,
     652|         "guides": cam.get("guides") or [],
     653|         "stepsVersion": cam.get("install_version") or "",
     654|         "stepsSource": cam.get("install_source") or "",
     655|         "source": cam.get("source_url") or "",
     656|         "summary": cam.get("summary") or "",
     657|         "links": links,
     658|         "monitors": cam.get("compatible_monitors") or [],
     659|         "prev": cam.get("previous_versions") or [],
     660|     }
     661| 
     662| 
     663| def panel_links(cam):
     664|     """The same three labels the plate uses, for the panel's action row."""

